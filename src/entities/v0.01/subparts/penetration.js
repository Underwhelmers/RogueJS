class Penetration {
  constructor() {
    this.progress = 0.0; // How deep it went
    this.friction = 1.0; // Lowered by lube (vag-lube (0.4), mouth-lube(0.2), slime-lube(0.8))
    this.numbness = 0.0; // Increases with pain, lowers when teasing
    this.speed = 0.0;
  }
}